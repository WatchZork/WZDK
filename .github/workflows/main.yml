name: Quality & Security Gate

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches:
      - main
  workflow_dispatch:    

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  lint:
    name: MegaLinter (C/C++)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.CI_SUBMODULE_PAT }}
          submodules: recursive
          fetch-depth: 0

      - name: Run MegaLinter
        uses: oxsecurity/megalinter/flavors/c_cpp@v7
        env:
          VALIDATE_ALL_CODEBASE: false
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ENABLE_LINTERS: C_CPP_CLANG_FORMAT,C_CPP_CPPCHECK,C_CPP_CPPLINT
          APPLY_FIXES: none
          GIT_AUTHOR_NAME: "WatchZork Security Bot"
          GIT_AUTHOR_EMAIL: "devsecops@watchzork.local"
          DISABLE_LINTERS: REPOSITORY_SECRETS
          GRYPE_DB_AUTO_UPDATE: "true"
          GRYPE_DB_VALIDATE_AGE: "false"

  sast:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    container:
      image: returntocorp/semgrep
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.CI_SUBMODULE_PAT }}
          submodules: recursive

      - name: Run Semgrep
        run: |
          semgrep ci \
            --config="p/c" \
            --config="p/cpp" \
            --sarif --output=semgrep.sarif
            
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif   
